---
import "../styles/articles.css";

const {
  eyebrow = "Kjekt å vite",
  title,
  intro,
} = Astro.props;
---

<section class="article-hero">
  <div class="container article-hero-inner">
    <p class="eyebrow">{eyebrow}</p>
    <h1>{title}</h1>
    <p>{intro}</p>
  </div>
</section>
